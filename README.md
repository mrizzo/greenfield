# nightscout-dl

A small utility related to Nightscout downloads.

This repository was initialized from an existing local workspace. It contains:

- `nightscout-dl.sh` - main script
- `install.sh` - installer script
- `config` - configuration file

See `nightscout-dl.sh` for usage details.
